# FA Library 测试记录

## 1. 测试信息

| 项目 | 内容 |
|---|---|
| 测试日期 | 2026-08-10 |
| 测试分支 | `dev` |
| 前端 | Vue 3、Vite、Playwright、Chromium |
| 后端 | Flask、Pytest、SQLite 内存测试数据库 |
| 业务数据库 | MySQL 8.4 |
| 前端测试结果 | 7 passed |
| 后端测试结果 | 9 passed |

## 2. 前端端到端测试

测试文件：`front/e2e/case-flows.spec.js`

测试使用 Playwright 启动真实 Vue 页面，并通过网络拦截提供确定性的 API Mock。这样可以验证完整浏览器交互，同时避免测试修改开发 MySQL 数据。

执行命令：

```powershell
cd D:\user\yym\fa\front
pnpm run test:e2e
```

### E2E-001 新增 Case

前置条件：主页加载完成，Mock 返回下拉选项和下一个 Case ID。

步骤：

1. 点击“新增”；
2. 检查 Case ID 是否为接口返回的 `FA20260810013`；
3. 选择 Project、Product、Technology；
4. 输入 Fail Mode，Fail Type 留空；
5. 点击“提交”。

预期结果：

- 下拉选项来自 `/api/v1/cases/options`；
- Case ID 来自 `/api/v1/cases/next-case-id`；
- Fail Type 留空仍能提交；
- 提交后返回列表并显示新增 Case。

实际结果：通过。

### E2E-002 查看 Case 详情

步骤：

1. 点击列表中的 Case ID；
2. 检查 Information 字段；
3. 检查右上角操作按钮。

预期结果：进入只读详情页，字段不可编辑，右上角显示“编辑”。

实际结果：通过。

### E2E-003 编辑 Case

步骤：

1. 点击 Operation 中的编辑图标；
2. 修改 Product 和 Fail Mode；
3. 点击“保存”。

预期结果：进入页面式编辑界面；Case ID 只读；保存后显示新值并恢复只读状态。

实际结果：通过。

### E2E-004 搜索

步骤：在搜索框输入 `Mode 12`。

预期结果：请求携带 `keyword=Mode 12`，列表仅显示匹配的 Case。

实际结果：通过。

### E2E-005 分页

步骤：点击页码 `2`。

预期结果：请求第二页数据，列表显示第 11、12 条 Case，不再显示第一页数据。

实际结果：通过。

### E2E-006 批量删除

步骤：

1. 勾选前两条 Case；
2. 点击工具栏“删除”；
3. 确认删除提示。

预期结果：调用批量删除接口，刷新后两条记录不再显示。

实际结果：通过。

### E2E-007 数据字典同步

步骤：

1. 进入System Management > Data Dictionary；
2. 编辑Project字典，将第一个选项修改为`Phoenix`；
3. 保存后返回FA Library；
4. 打开新增Case页面并检查Project下拉框。

预期结果：

- 数据字典表格显示更新后的`Phoenix`；
- 新增Case的Project下拉框同步显示`Phoenix`；
- 编辑弹窗保存和删除确认均使用站内组件。

实际结果：通过。

前端测试输出：

```text
Running 7 tests using 1 worker
7 passed
```

## 3. 后端接口测试

测试文件：`back/tests/test_cases.py`

执行命令：

```powershell
cd D:\user\yym\fa\back
.\.venv\Scripts\python.exe -m pytest -q
```

| 编号 | 测试内容 | 预期结果 | 实际结果 |
|---|---|---|---|
| API-001 | 连续新增两个 Case | 自动生成同日期的 `001`、`002` 编号 | 通过 |
| API-002 | Fail Type 为空 | 创建成功并保存为空 | 通过 |
| API-003 | Fail Mode 为空 | 返回 400 字段校验错误 | 通过 |
| API-004 | 查询并编辑 Case | 数据回显、修改成功、Case ID 不变 | 通过 |
| API-005 | 获取默认选项 | 返回 Project1~3、Product1~3、Technology1~3 | 通过 |
| API-006 | 搜索 Case | 仅返回关键词匹配记录 | 通过 |
| API-007 | 批量删除 | 返回正确删除数量 | 通过 |

后端测试输出：

```text
.........
9 passed
```

## 4. 测试过程中发现并修复的问题

| 问题 | 原因 | 修复 |
|---|---|---|
| Fail Type 留空时浏览器阻止提交 | 前端仍存在 `required` | 移除新增和编辑页面的必填属性 |
| 点击 Case ID 打开编辑弹窗 | Case ID 与编辑按钮共用事件 | 拆分为只读详情事件和编辑事件 |
| Operation 编辑页面不符合设计 | 仍使用旧模态弹窗 | 改为与新增页一致的页面式编辑界面 |
| SQLite 测试新增失败 | SQLite 不支持 BIGINT 主键自动递增语义 | 为 SQLite 增加 Integer 类型映射 |
| Playwright 服务启动超时 | Vite 启动命令多传递了 `--` | 改用 `pnpm exec vite --host ...` |
| Case连续编辑时报unknown fields | 后端返回的id、created_at、updated_at被合并进表单并再次提交 | 请求层只保留可编辑字段，回填前清除元数据 |
| 删除使用浏览器原生confirm | 页面交互依赖window.confirm | 新增统一ConfirmDialog站内确认弹窗 |
| Product过多时图例覆盖搜索栏 | 图例没有高度限制 | 图例限制在卡片内部并支持纵向滚动，长名称省略显示 |
| Cases by Project看不到Y轴 | 仅有网格线，没有渲染轴线和刻度 | 增加Y轴纵线及最大值、中间值、零点动态刻度 |

## 5. 数据库迁移验证

已执行迁移：

```text
084d0727ee15 -> 9b6f3d24a1c7
make fail_type optional
```

结果：MySQL 的 `fa_cases.fail_type` 已允许为空。

## 6. 数据字典增量测试记录

测试日期：2026-08-10

### 后端测试

测试文件：

- `back/tests/test_cases.py`
- `back/tests/test_data_dictionaries.py`

| 编号 | 测试内容 | 预期结果 | 实际结果 |
|---|---|---|---|
| DD-API-001 | 首次查询数据字典 | 返回Project、Product、Technology默认选项 | 通过 |
| DD-API-002 | 替换Project字典 | 保存新选项，Case options接口同步返回 | 通过 |
| DD-API-003 | 非法字典类型 | 返回400校验错误 | 通过 |
| DD-API-004 | 重复或空选项 | 返回400校验错误 | 通过 |
| DD-API-005 | 清空字典选项 | 保存空列表，Case options同步为空 | 通过 |

执行结果：`9 passed`。

### 前端验证

| 编号 | 测试内容 | 预期结果 | 实际结果 |
|---|---|---|---|
| DD-UI-001 | 数据字典菜单导航 | 两组菜单正常显示，当前页面高亮 | 通过 |
| DD-UI-002 | 编辑字典并保存 | 表格更新且Case下拉选项同步 | 通过 |
| DD-UI-003 | 连续两次编辑Case | 不提交id、created_at、updated_at，无unknown fields错误 | 通过 |
| DD-UI-004 | 批量删除确认 | 显示站内确认弹窗，不调用浏览器confirm | 通过 |
| DD-UI-005 | Product图例过多 | 图例在卡片内部滚动，不覆盖搜索栏 | 通过 |
| DD-UI-006 | Project柱状图Y轴 | 显示轴线和动态刻度 | 通过 |

验证结果：

- Playwright：`7 passed`；
- Vite生产构建：通过；
- 代码检查：未发现`window.confirm`调用；
- Alembic迁移头：`c3a4f5d6e7b8`。

## 7. 后续测试建议

- 增加真实 Flask + MySQL 的联调测试环境；
- 增加接口异常、网络超时和重复提交场景；
- 增加移动端视口测试；
- 在 GitHub Actions 中自动运行 `pytest`、前端构建和 Playwright；
- 每次发布前将最新执行时间、提交号和测试结果追加到本文档。
