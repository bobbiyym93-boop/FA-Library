
# 新增FA Case
1. Case ID每次自动生成，生成规则“FA" + 日期（20260710）+编号（000-999）
2. Project为下拉选择，选项由数据字典中的Project维护，默认显示Project1、Project2、Project3，必填，string
3. Product为下拉选择，选项由数据字典中的Product维护，默认显示Product1、Product2、Product3，必填，string
4. Technology为下拉选择，选项由数据字典中的Technology维护，默认显示Technology1、Technology2、Technology3，必填，string
5. Fail Type为用户输入，string，非必填
6. Fail Mode为用户输入，string，必填

# 编辑FA Case
1. 每个case可以点击operation中编辑按键进行编辑页面，编辑页面与新增页面相同，只是回显每个case的列内容

# 查看FA Case
1. 点击每个进入查看页面，查看时不可编辑，但是可以点击右上角编辑按键进行编辑

# 数据字典
1. 左侧菜单栏保留Library > FA Library，并新增System Management > Data Dictionary。
2. Data Dictionary页面以表格展示Project、Product、Technology三类字典及其选项。
3. 支持新增、编辑和删除字典选项；编辑时回显当前选项，可逐项增加或移除。
4. 字典数据保存到数据库，FA Case新增和编辑页面的下拉框读取同一数据源。
5. 字典更新后，FA Case表单下拉选项应同步刷新。
6. 删除Case、批量删除Case及删除字典选项时使用站内确认弹窗，不使用浏览器原生提示。

# Dashboard图表
1. Product Distribution在Product数量较多时，图例限制在卡片内部并支持滚动，不能覆盖下方搜索栏。
2. Product名称过长时显示省略号，鼠标悬停可查看完整名称、数量及占比。
3. Cases by Project必须显示Y轴纵线及最大值、中间值、零点刻度，刻度随数据范围自动变化。

# 数据校验
1. FA Case新增和更新接口只提交project、product、technology、fail_type、fail_model字段。
2. id、created_at、updated_at等只读元数据不得随编辑请求提交。
